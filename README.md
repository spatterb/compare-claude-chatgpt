# compare-claude-chatgpt


```bash
    code --list-extensions --show-versions > claude-extentions.txt
    cat /home/vscode/.vscode-remote/data/Machine/settings.json > claude-settings.json

    code --list-extensions --show-versions > chat-extentions.txt
    cat /home/vscode/.vscode-remote/data/Machine/settings.json > chat-settings.json

    which python
    python --version

```
